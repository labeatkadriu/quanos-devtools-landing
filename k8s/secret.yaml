# Replace the values below before applying. They are placeholders only.
#   echo -n 'your-secret' | base64
apiVersion: v1
kind: Secret
metadata:
  name: devtools-secrets
  namespace: devtools
type: Opaque
stringData:
  JWT_SECRET: replace-with-a-long-random-string
  ADMIN_PASSWORD: replace-with-a-strong-password
